class AppDimension {
  //Text size
  /// Tiny text size - 12px - Used for very small labels, footnotes, etc.
  static const double tinyTextSize = 12;

  /// Small text size - 14px - Used for secondary information, captions, etc.
  static const double smallTextSize = 14;

  /// Body text size - 16px - Default text size for regular content
  static const double bodyTextSize = 16;

  /// Large text size - 20px - Used for section headers, important information
  static const double largeTextSize = 20;

  /// Extra large text size - 24px - Used for sub-headers, emphasized content
  static const double extraLargeTextSize = 24;

  /// Header line text size - 32px - Used for main headers, titles
  static const double headerLineTextSize = 32;

  //Padding
  /// Large padding - 24px - Used for screen margins, major section separations
  static const double defaultLPadding = 24;

  /// Default padding - 16px - Standard padding for most UI elements
  static const double defaultPadding = 16;

  /// Small padding - 12px - Used for tighter spacing between related elements
  static const double defaultSPadding = 12;

  /// Extra small padding - 10px - Used for compact UI elements
  static const double defaultXSPadding = 10;

  /// Extra extra small padding - 8px - Minimal padding for very compact layouts
  static const double defaultXXSPadding = 8;

  //IconSize
  /// Extra extra extra small icon size - 18px - For very tiny icons
  static const double kDefaultXXXSIconSize = 18;

  /// Extra extra small icon size - 20px - For very small icons
  static const double kDefaultXXSIconSize = 20;

  /// Extra small icon size - 24px - For small icons, common in dense UIs
  static const double kDefaultXSIconSize = 24;

  /// Small icon size - 28px - For standard small icons
  static const double kDefaultSmallIconSize = 28;

  /// Large icon size - 48px - For prominent icons
  static const double defaultLargeIconSize = 48;

  /// Extra large icon size - 64px - For featured or hero icons
  static const double defaultExtraLargeIconSize = 64;

  //Spacing
  /// Extra small spacing - 4px - Minimal spacing between tightly related elements
  static const double defaultXSspacing = 4;

  /// Default spacing - 8px - Standard spacing for most UI elements
  static const double defaultSpacing = 8;

  /// Medium spacing - 12px - Medium gap between related elements
  static const double defaultMSpacing = 12;

  /// Large spacing - 16px - Larger gap between sections or groups
  static const double defaultLSpacing = 16;

  /// Extra large spacing - 24px - Major separation between content blocks
  static const double defaultXLSpacing = 24;

  //Border radius
  /// Circle radius border - 156px - For fully circular elements
  static const double circleRadiusBorder = 156;

  /// Large radius - 32px - For very rounded corners
  static const double defaultLRadius = 32;

  /// Default radius - 16px - Standard corner radius for most UI elements
  static const double defaultRadius = 16;

  /// Large-small radius - 12px - Intermediate corner radius
  static const double defaultLSRadius = 12;

  /// Small radius - 8px - Subtle corner radius for compact elements
  static const double defaultSRadius = 8;

  //Size
  /// Bottom navigation bar height - 48px - Fixed height for bottom navigation
  static const double kBottomNavigationBarHeight = 48;

  /// Close icon size - 32px - Size for close/dismiss icons
  static const double kCloseIconSize = 32;

  /// Default large icon height - 96px - Height for large featured icons
  static const double kDefaultLargeIconHeight = 96;

  /// AppBar avatar size - 48px - Size for avatar icons in app bars
  static const double kAppBarAvatarSize = 48;
}
